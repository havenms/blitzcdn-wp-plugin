{
  "name": "blitzcdn-middleware",
  "version": "2.0.0",
  "description": "Middleware service for BlitzCDN zip-based media migration with Redis queue",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "start:legacy": "bun run server.ts"
  },
  "dependencies": {
    "node-appwrite": "^14.0.0",
    "redis": "^5.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
